# 🏗️ Architecture Overview - UnityKt Multi-Platform Support

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                      Your Kotlin Application                     │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              UnityAssetManager API                          │ │
│  │  • loadFile()                                               │ │
│  │  • loadByteArray()                                          │ │
│  │  • loadFolder()                                             │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              ↓                                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │           TextureDecoder (Java + JNI)                       │ │
│  │  • decodeDXT1()                                             │ │
│  │  • decodeDXT5()                                             │ │
│  │  • decodeETC1/2()                                           │ │
│  │  • decodeASTC() ... etc                                     │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              ↓                                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              NativeUtils (Platform Loader)                  │ │
│  │                                                              │ │
│  │  1. Detect Platform (os.name + os.arch)                    │ │
│  │     • Windows → "windows-x86_64"                            │ │
│  │     • Linux   → "linux-x86_64"                              │ │
│  │     • macOS   → "darwin-x86_64" or "darwin-aarch64"        │ │
│  │                                                              │ │
│  │  2. Construct Library Name                                  │ │
│  │     • Windows → "TextureDecoder.dll"                        │ │
│  │     • Linux   → "libTextureDecoder.so"                      │ │
│  │     • macOS   → "libTextureDecoder.dylib"                   │ │
│  │                                                              │ │
│  │  3. Load from JAR: /natives/{platform}/{library}            │ │
│  │                                                              │ │
│  │  4. Extract to temp directory                               │ │
│  │                                                              │ │
│  │  5. System.load(absolutePath)                               │ │
│  └────────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              Native C++ Library (Platform Specific)              │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  TextureDecoderNative                                       │ │
│  │                                                              │ │
│  │  • bcn.cpp  (DXT1/3/5, BC4/5/6/7)                           │ │
│  │  • etc.cpp  (ETC1, ETC2, EAC)                               │ │
│  │  • astc.cpp (ASTC)                                          │ │
│  │  • pvrtc.cpp (PVRTC)                                        │ │
│  │  • atc.cpp  (ATC)                                           │ │
│  │  • crunch.cpp / unitycrunch.cpp                             │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## Build Process Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         Source Code                              │
│                                                                   │
│  • io_github_deficuet_unitykt_extension_TextureDecoder.cc       │
│  • lib/*.cpp (astc, bcn, etc, pvrtc, atc, crunch, etc.)         │
│  • lib/*.h                                                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      CMake Configuration                         │
│                                                                   │
│  1. Detect OS & Architecture                                     │
│  2. Find JDK (JNI headers)                                       │
│  3. Set compiler flags                                           │
│  4. Configure output directory                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      Platform Build                              │
│                                                                   │
│  Linux:    build-linux.sh    → GCC/Clang                        │
│  Windows:  build-windows.bat → MSVC (Visual Studio)             │
│  macOS:    build-darwin.sh   → Apple Clang                      │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                   Compiled Native Libraries                      │
│                                                                   │
│  src/main/resources/natives/                                     │
│  ├── linux-x86_64/libTextureDecoder.so                          │
│  ├── windows-x86_64/TextureDecoder.dll                          │
│  ├── darwin-x86_64/libTextureDecoder.dylib                      │
│  └── darwin-aarch64/libTextureDecoder.dylib                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      Gradle Build                                │
│                                                                   │
│  ./gradlew build                                                 │
│  • Compiles Kotlin/Java code                                     │
│  • Packages native libraries into JAR                            │
│  • Creates executable JAR with all dependencies                  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      Final JAR File                              │
│                                                                   │
│  build/libs/UnityKt-1.0.0.jar                                   │
│  • Contains all Java/Kotlin classes                              │
│  • Contains all native libraries                                 │
│  • Works on any platform!                                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## Runtime Loading Flow

```
Application Starts
     ↓
TextureDecoder class loaded
     ↓
Static initializer runs
     ↓
┌────────────────────────────────────────┐
│ Try: System.loadLibrary("TextureDecoder")│
│ (Looks in java.library.path)            │
└────────────────────────────────────────┘
     ↓
   Success? ──Yes──→ ✅ Library Ready
     │
     No (UnsatisfiedLinkError)
     ↓
┌──────────────────────────────────────────┐
│ NativeUtils.loadLibraryFromJar()         │
└──────────────────────────────────────────┘
     ↓
┌──────────────────────────────────────────┐
│ 1. Detect Platform                       │
│    os.name = "Linux"                     │
│    os.arch = "amd64"                     │
│    → platform = "linux-x86_64"           │
└──────────────────────────────────────────┘
     ↓
┌──────────────────────────────────────────┐
│ 2. Build Library Name                    │
│    "libTextureDecoder.so"                │
└──────────────────────────────────────────┘
     ↓
┌──────────────────────────────────────────┐
│ 3. Construct Resource Path               │
│    "/natives/linux-x86_64/               │
│     libTextureDecoder.so"                │
└──────────────────────────────────────────┘
     ↓
┌──────────────────────────────────────────┐
│ 4. Load from JAR                         │
│    getResourceAsStream(resourcePath)     │
└──────────────────────────────────────────┘
     ↓
   Found? ──No──→ ❌ UnsatisfiedLinkError
     │            "Native library not found"
     Yes
     ↓
┌──────────────────────────────────────────┐
│ 5. Create Temp File                      │
│    /tmp/UnityKt_Deficuet/                │
│    TextureDecoder_12345.so               │
└──────────────────────────────────────────┘
     ↓
┌──────────────────────────────────────────┐
│ 6. Copy Stream to Temp File              │
│    Files.copy(inputStream, tempFile)     │
└──────────────────────────────────────────┘
     ↓
┌──────────────────────────────────────────┐
│ 7. Load Library                          │
│    System.load(tempFile.absolutePath)    │
└──────────────────────────────────────────┘
     ↓
   Success? ──No──→ ❌ UnsatisfiedLinkError
     │            "Failed to load library"
     Yes
     ↓
┌──────────────────────────────────────────┐
│ 8. Cleanup (POSIX) or DeleteOnExit       │
└──────────────────────────────────────────┘
     ↓
✅ Native Library Ready!
     ↓
JNI Methods Available
     ↓
TextureDecoder.decodeDXT1() etc. work!
```

---

## Platform Detection Logic

```
┌──────────────────────────────────┐
│    System.getProperty("os.name") │
└──────────────────────────────────┘
              ↓
┌─────────────┬────────────┬────────────┐
│  "Windows"  │   "Linux"  │  "Mac OS X"│
│  (or "win") │ (or "nux") │(or "darwin")│
└─────────────┴────────────┴────────────┘
       ↓             ↓            ↓
   "windows"      "linux"     "darwin"
       ↓             ↓            ↓
┌─────────────────────────────────────────┐
│  System.getProperty("os.arch")          │
└─────────────────────────────────────────┘
              ↓
┌────────────┬─────────────┬──────────┐
│  "amd64"   │  "aarch64"  │  "arm"   │
│ "x86_64"   │  "arm64"    │          │
└────────────┴─────────────┴──────────┘
       ↓            ↓           ↓
   "x86_64"    "aarch64"    "arm"
       ↓            ↓           ↓
┌─────────────────────────────────────────┐
│        Platform Identifier              │
│  • "windows-x86_64"                     │
│  • "linux-x86_64"                       │
│  • "darwin-x86_64"                      │
│  • "darwin-aarch64"                     │
└─────────────────────────────────────────┘
```

---

## Library Naming Convention

```
┌────────────────────────────────────────────┐
│              Platform                       │
└────────────────────────────────────────────┘
              ↓
┌──────────────┬───────────────┬────────────┐
│   Windows    │     Linux     │   macOS    │
└──────────────┴───────────────┴────────────┘
       ↓              ↓              ↓
┌──────────────┬───────────────┬────────────┐
│  No prefix   │  "lib" prefix │"lib" prefix│
│  .dll ext    │  .so ext      │.dylib ext  │
└──────────────┴───────────────┴────────────┘
       ↓              ↓              ↓
┌──────────────┬───────────────┬────────────┐
│ TextureDecoder│libTextureDecoder│libTextureDecoder│
│    .dll      │     .so       │   .dylib   │
└──────────────┴───────────────┴────────────┘
```

---

## Directory Structure

```
UnityKt/
├── .github/
│   └── workflows/
│       └── build-natives.yml          # CI/CD automation
│
├── TextureDecoderNative/
│   ├── CMakeLists.txt                 # Cross-platform build
│   ├── build-linux.sh                 # Linux build script
│   ├── build-windows.bat              # Windows build script
│   ├── build-darwin.sh                # macOS build script
│   ├── build-linux-docker.sh/bat      # Docker cross-compile
│   ├── io_github_...TextureDecoder.h  # JNI header
│   ├── io_github_...TextureDecoder.cc # JNI implementation
│   ├── README.md                      # Native lib docs
│   └── lib/
│       ├── astc.cpp/h                 # ASTC decoder
│       ├── bcn.cpp/h                  # BCN decoders
│       ├── etc.cpp/h                  # ETC decoders
│       ├── pvrtc.cpp/h                # PVRTC decoder
│       ├── atc.cpp/h                  # ATC decoder
│       ├── crunch.cpp/h               # Crunch
│       ├── unitycrunch.cpp/h          # Unity Crunch
│       └── ...                        # Other support files
│
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── io/github/deficuet/unitykt/extension/
│   │   │       └── TextureDecoder.java        # JNI interface
│   │   ├── kotlin/
│   │   │   └── io/github/deficuet/unitykt/
│   │   │       ├── internal/
│   │   │       │   └── NativeUtils.kt         # Platform loader
│   │   │       └── ...                        # Other classes
│   │   └── resources/
│   │       └── natives/
│   │           ├── linux-x86_64/
│   │           │   └── libTextureDecoder.so   # Linux lib
│   │           ├── windows-x86_64/
│   │           │   └── TextureDecoder.dll     # Windows lib
│   │           ├── darwin-x86_64/
│   │           │   └── libTextureDecoder.dylib# macOS Intel
│   │           └── darwin-aarch64/
│   │               └── libTextureDecoder.dylib# macOS ARM
│   └── test/
│       └── kotlin/
│           └── io/github/deficuet/unitykt/test/
│               └── NativeLibraryTest.kt       # Tests
│
├── BUILD.md                          # Build instructions
├── QUICKSTART.md                     # Quick start guide
├── LINUX_SERVER_GUIDE.md             # Server deployment
├── EXAMPLE_API.md                    # Code examples
├── NATIVE_SETUP.md                   # Technical details
├── SUMMARY.md                        # Change summary
├── DOCUMENTATION_INDEX.md            # This index
├── PULL_REQUEST_TEMPLATE.md          # PR template
└── README.md                         # Main README
```

---

## Deployment Flow

```
Development Machine                      Linux Server
─────────────────                       ────────────

┌──────────────────┐
│ Build Native Lib │
│ ./build-linux.sh │
└────────┬─────────┘
         ↓
┌──────────────────┐
│  ./gradlew build │
│ (Creates JAR)    │
└────────┬─────────┘
         ↓
┌──────────────────┐
│ UnityKt.jar      │
│ • Java classes   │
│ • Kotlin classes │
│ • Native libs:   │
│   - Linux .so    │
│   - Win .dll     │
│   - macOS .dylib │
└────────┬─────────┘
         ↓
         │ scp/deploy
         ├──────────────────────────────→ ┌─────────────────┐
         │                                 │  UnityKt.jar    │
         │                                 └────────┬────────┘
         │                                          ↓
         │                                 ┌─────────────────┐
         │                                 │ java -jar       │
         │                                 │ UnityKt.jar     │
         │                                 └────────┬────────┘
         │                                          ↓
         │                                 ┌─────────────────┐
         │                                 │ Platform: Linux │
         │                                 │ Loads: .so      │
         │                                 └────────┬────────┘
         │                                          ↓
         │                                 ┌─────────────────┐
         │                                 │ ✅ Running!     │
         │                                 └─────────────────┘
```

---

## Success Indicators

```
✅ Cross-Platform Support
   • Windows x86_64
   • Linux x86_64
   • macOS x86_64
   • macOS ARM64

✅ Automatic Platform Detection
   • OS detection
   • Architecture detection
   • Library name mapping

✅ Smart Loading
   • Try system library first
   • Fall back to JAR resources
   • Extract to temp
   • Clean up properly

✅ Single JAR Distribution
   • All platforms in one JAR
   • No external dependencies
   • Works everywhere

✅ Complete Documentation
   • Quick start guide
   • Build instructions
   • Server deployment guide
   • Code examples
   • Technical details

✅ CI/CD Ready
   • GitHub Actions workflow
   • Automated builds
   • Artifact generation

✅ Tested & Verified
   • Comprehensive test suite
   • All texture formats
   • Error handling
   • Multiple platforms
```

---

This architecture enables UnityKt to work seamlessly across all platforms with a single JAR file! 🎉
